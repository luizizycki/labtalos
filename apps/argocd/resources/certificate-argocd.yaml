apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: argocd-tls
  namespace: default
spec:
  secretName: labtalos-tls
  dnsNames:
    - argocd.izycki.com.br
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
